node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
.env
.env.*
!.env.example
# Committed: production build env (no secrets) — Docker/Railway build reads it.
!.env.production

# Self-hosted MediaPipe assets are fetched/copied at build time by
# scripts/fetch-mediapipe-assets.mjs — do NOT commit them (the model is ~3.7MB
# and the WASM is ~5.5MB+). They are regenerated by `pnpm build` (prebuild).
public/mediapipe/
# HDRI for drei Environment — fetched at build time by scripts/fetch-mediapipe-assets.mjs
public/hdri/
